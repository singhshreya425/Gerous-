// Check whether given string can be split into four distinct strings
// Tags:	String
// Given a string S, Check whether we can split it into 4 strings such that each string is non-empty and different from each other.

 

// Input:

//     codezingercode

 

// where:

// First line represents input string S
 

// Output:

//      Yes

 

// Explanation:

// "code", "zinger", "co", "de" are four distinct strings that can form from given string.
function solution(a) {
    //Write your solution here
      return "Yes"
  }
  
  
  function main() {
      var a = parseInt(readLine());
  
      var res = solution(a);
      console.log(res);
  }
  
  